{{!-- Start Nav --}}
<div class="row">
    <div class="col-md-12 grid-margin">
        <div class="card">
            <div class="template-demo">
                <nav aria-label="breadcrumb">
                    <ol class="breadcrumb">
                        <li class="breadcrumb-item"><a href="/state/list">Home</a></li>
                        <li class="breadcrumb-item"><a href="/access-level/list">Lista de Accesos</a></li>
                        {{!-- <li class="breadcrumb-item"><a href="#">Data</a></li> --}}
                        <li class="breadcrumb-item active" aria-current="page">Modificar Nivel de Acceso</li>
                    </ol>
                </nav>
            </div>
        </div>
    </div>
</div>
{{!--End Nav --}}

{{!-- Start Form --}}
<div class="row grid-margin">
    <div class="col-lg-12">
        <div class="card">
            <div class="card-body">
                <h4 class="card-title text-center">Modificar Nivel de Acceso</h4>
                <form action="/access-level/{{accessLevel.access_level_id}}/update" method="POST">
                    <fieldset>
                        <div class="form-group">
                            <label for="cname">Nombre</label>
                            <input id="cname" class="form-control" name="access_name" value="{{accessLevel.access_name}}" minlength="2" type="text" placeholder="Escribir titulo del acceso..." required>
                        </div>
                        <div class="form-group">
                            <label for="ccomment">Descripci&oacute;n</label>
                            <textarea id="ccomment" class="form-control" name="description" required>{{accessLevel.description}}</textarea>
                        </div>
                        <div class="form-group">
                            <label>Estado</label>
                            {{#showFormElements accessLevel.active}}
                            <select class="js-example-basic-single w-100" name="active">
                                <option value="1">Activo</option>
                                <option value="0">Inactivo</option>
                            </select>
                            {{else}}
                            <select class="js-example-basic-single w-100" name="active">
                                <option value="0">Inactivo</option>
                                <option value="1">Activo</option>
                            </select>
                            {{/showFormElements}}
                        </div>
                        <button class="btn btn-primary" type="submit">Modificar</button>
                    </fieldset>
                </form>
            </div>
        </div>
    </div>
</div>
{{!-- End Form --}}